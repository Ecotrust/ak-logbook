[uwsgi]

workers = <%= num_cpus %>

chdir = /usr/local/apps/formhub
env = DJANGO_SETTINGS_MODULE=formhub.settings
module = django.core.handlers.wsgi:WSGIHandler()

vhost = true
# Don't use python plugin if relying on pip version of uwsgi
# plugins = python
virtualenv = /usr/local/venv/aklogbook