; ==============================================
;  celery flower service 
;  https://github.com/mher/flower
; ==============================================

[program:celeryflower]
command=/usr/local/venv/aklogbook/bin/python /usr/local/apps/formhub/manage.py celery flower --url_prefix=flower
directory=/usr/local/apps/land_owner_tools/lot
user=<%= user %>
numprocs=1
stdout_logfile=/tmp/celeryflower.log
stderr_logfile=/tmp/celeryflower.log
autostart=true
autorestart=true
startsecs=10

; Need to wait for currently executing tasks to finish at shutdown.
; Increase this if you have very long running tasks.
stopwaitsecs = 600
